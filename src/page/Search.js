import { Store } from "../UI/Body/Gender/Store";

const Search = () => {
  const clothes = [Store.men.Clothes, Store.women.Clothes, Store.kids.Clothes];
  const furniture = [Store.men.Furniture];
  const shoes = [Store.men.Shoes, Store.women.Shoes];
  const bag = [Store.women.Bags];
  const electronics = [Store.men.Electronics];
  const jewelry = [Store.women.Jewelry];
  const toys = [Store.kids.Toys];

  return {};
};

export default Search;
